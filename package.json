{
  "name": "lms-app",
  "private": true,
  "description": "Công cụ Node chạy trên MÁY (npm ci): wrangler — publish_cf.sh deploy, src/main.py ghi nhịp tim KV; tailwindcss — builder dựng CSS sẵn (ADR 0048). Ghim phiên bản để không tải mới mỗi lần. Workers Builds đã tắt (ADR 0003); publish_app.sh chép file này sang repo app làm bản lưu.",
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "wrangler": "4.128.0"
  }
}
